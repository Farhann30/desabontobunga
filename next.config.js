/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost', 'example.com', 'mydomain.com'], // Menambahkan lebih banyak domain
  },
};

module.exports = nextConfig;
